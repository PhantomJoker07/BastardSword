using System;
using System.Collections.Generic;
using System.Text;

namespace GMath
{
    public static partial class Gfx
    {
        public const float pi = (float)(Math.PI);
        public const float two_pi = (float)(2 * Math.PI);
        public const float pi_over_4 = (float)(Math.PI * 0.25);
        public const float inverse_pi = (float)(1.0 / Math.PI);
        public const float inverse_two_pi = (float)(0.5 / Math.PI);
        public const float e_constant = (float)Math.E;
    }
}
